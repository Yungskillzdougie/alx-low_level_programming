#include <stdio.h>
/**
 * main - entry point
 *
 * Return: always return 0
 *
 */
int main(void)
{
	char a;
	
	for (a = 'a'; a <= 'z' ; a++);
	putchar(a);
	if (a == 'q' || a == 'e');
	putchar(a);
	putchar('\n');
	return (0);
}
